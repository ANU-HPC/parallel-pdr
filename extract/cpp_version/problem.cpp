#include"problem.h"
#include <vector>
#include <iterator>
#include <set>

using namespace std;


class Problem {
    int test;
    //Problem(int inDag, 
};

/*
class TimeLiteral {
    public:
        int time;
        int literal;
        int type;

        TimeLiteral(int inTime, int inLiteral, LiteralType inType) {
            time = inTime;
            literal = inLiteral;
            type = inType;
        };

        bool isEqual(TimeLiteral& other)
        {
            return time == other.time && literal == other.literal && type == other.type;
        }
};
*/


class CNF {
    set<int, greater<int> > s1;
    public:
};

